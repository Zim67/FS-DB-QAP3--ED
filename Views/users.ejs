<!-- users.ejs -->

<% if (users && users.length > 0) { %>
    <h1>User List</h1>
    
    <ul>
      <% users.forEach((user) => { %>
      <li><%= user.name %> - <%= user.email %></li>
      <% }); %>
    </ul>
    <% } else { %>
    <p>No users found.</p>
    <% } %>